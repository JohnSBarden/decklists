---
import Layout from '../layouts/Layout.astro';
import Navigation from '../components/Navigation.astro';
---

<Layout title="About - Johnny's Decks">
  <Navigation />
  <div class="relative min-h-screen flex items-center justify-center overflow-hidden">
    <!-- Animated background -->
    <div class="absolute inset-0">
      <div class="absolute inset-0 [background:var(--gradient-background)]"></div>
      <div class="absolute inset-0 opacity-30">
        <div class="absolute inset-0 [background:var(--gradient-pulse)] animate-pulse"></div>
      </div>
      <!-- Magic card pattern -->
      <div class="absolute inset-0 opacity-5">
        <div class="h-full w-full bg-[url('/mtg_pentagon_mana.svg')] bg-center bg-no-repeat bg-[length:500px_500px] animate-spin-slow"></div>
      </div>
    </div>

    <main class="relative z-10 container mx-auto px-4 py-16">
      <div class="max-w-3xl mx-auto">
        <h1 class="text-4xl font-bold gradient-text [background:var(--gradient-text)] mb-8 text-center">
          About Me
        </h1>
        
        <div class="glass-card rounded-xl p-8 space-y-6">
          <section>
            <h2 class="text-2xl font-bold text-[var(--color-accent-primary)] mb-4">Who Am I?</h2>
            <p class="text-[var(--color-text-secondary)] leading-relaxed">
              I'm Johnny, a passionate Magic: The Gathering player who loves building creative and unique decks. 
              My approach to the game focuses on finding interesting card combinations and synergies that create 
              memorable gameplay experiences.
            </p>
          </section>

          <section>
            <h2 class="text-2xl font-bold text-[var(--color-accent-primary)] mb-4">My Magic Journey</h2>
            <p class="text-[var(--color-text-secondary)] leading-relaxed">
              I started playing Magic during the original Innistrad block and fell in love with the game's depth 
              and complexity. Since then, I've been exploring various formats and deck-building strategies, 
              always looking for new ways to express creativity through the game.
            </p>
          </section>

          <section>
            <h2 class="text-2xl font-bold text-[var(--color-accent-primary)] mb-4">Why This Site?</h2>
            <p class="text-[var(--color-text-secondary)] leading-relaxed">
              This website is my way of sharing my deck-building journey with the Magic community. Here, you'll 
              find detailed breakdowns of my decks, the thought process behind card choices, and insights into 
              different strategies across various formats.
            </p>
          </section>

          <div class="mt-8 flex justify-center">
            <a href="/decks" class="transform hover:scale-105 transition-all duration-300">
              <button class="px-8 py-3 [background:var(--gradient-button)] rounded-lg font-bold text-[var(--color-text-primary)] shadow-lg hover:shadow-[var(--color-button-hover-shadow)] hover:shadow-opacity-25 focus:outline-none focus:ring-2 focus:ring-[var(--color-button-focus-ring)] focus:ring-offset-2 focus:ring-offset-slate-900">
                Explore My Decks
              </button>
            </a>
          </div>
        </div>
      </div>
    </main>
  </div>
</Layout>

<style>
  .glass-card {
    background: rgba(23, 25, 35, 0.7);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.1);
  }

  @keyframes spin-slow {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  .animate-spin-slow {
    animation: spin-slow 60s linear infinite;
    transform-origin: center;
  }
</style>
