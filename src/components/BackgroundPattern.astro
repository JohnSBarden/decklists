<!-- A spinning 5 dot pattern -->
<div class="absolute inset-0">
  <div class="absolute inset-0 [background:var(--gradient-background)]"></div>
  <div class="absolute inset-0 opacity-30">
    <div class="absolute inset-0 [background:var(--gradient-pulse)] animate-pulse"></div>
  </div>
  <!-- Magic card pattern -->
  <div class="absolute inset-0 opacity-5">
    <div class="h-full w-full bg-[url('/mtg_pentagon_mana.svg')] bg-center bg-no-repeat bg-[length:500px_500px] animate-spin-slow"></div>
  </div>
</div>